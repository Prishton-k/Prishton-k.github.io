// Array Notation
// Digdarshan Kc
// Date
//
// Extra for Experts:
 

function setup() {
    createCanvas(windowWidth, windowHeight); 
}
  function draw() {
    // Generate random RGB values
    let r = random(255);
    let g = random(255);
    let b = random(255);
    // Set the background color
    background(r,g,b);
    circle(800,370,200);
    line( 800,0, 800, 780);
 
  }


