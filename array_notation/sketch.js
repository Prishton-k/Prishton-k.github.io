// Array Notation
// Digdarshan Kc
// Date
//
// Extra for Experts:


function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);
  circle(mouseX, mouseY,100,100);
}
